query orderDetails($userId: ID!) {
  user(userId: $userId) {
    club {
      orders {
        orderId
        deadlineDate
        deliveryDate
        totalVolume
        status
        numberOfParticipants
        participants {
          userId
          name
          volume
          cost
          cashback
        }
      }
    }
  }
}