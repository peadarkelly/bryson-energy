query orderDetails($userId: ID!) {
  user(userId: $userId) {
    club {
      orders {
        orderId
        deadlineDate
        totalVolume
        numberOfParticipants
        participants {
          userId
          name
          volume
          cost
          cashback
        }
      }
    }
  }
}